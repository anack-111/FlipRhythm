using UnityEngine;
using UnityEngine.EventSystems;

public class ButtonArea : MonoBehaviour
{
    //public void OnPointerDown(PointerEventData eventData)
    //{
    //    // 자식 중 NoteUI가 있으면 가장 위의 것 판정
    //    NoteUI note = GetComponentInChildren<NoteUI>();
    //    if (note != null)
    //    {
    //        note.OnJudge();
    //    }
    //}
}
